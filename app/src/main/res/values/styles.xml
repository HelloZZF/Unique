<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/blue3</item>
        <item name="colorPrimaryDark">@color/blue3</item>
        <item name="colorAccent">@color/blue3</item>
    </style>

    <style name="UniqueTheme" parent="AppTheme"></style>

    <style name="myTabLayoutTextStyle" parent="TextAppearance.Design.Tab">

        <item name="textAllCaps">false</item>
    </style>

    <style name="StartTheme" parent="AppTheme">
        <!--<item name="android:statusBarColor">@android:color/transparent</item>-->
        <item name="android:windowBackground">@drawable/welcomebg</item>
    </style>

</resources>
